// ast.8g - 抽象構文木の定義
// Rustのenum構造を、動的なオブジェクト定義に置き換え

型 命令 = {
    種別: 文字列, // "変数宣言", "代入文", "もし文", "繰り返し文", "表示文" など
    内容: 辞書     // 各命令に固有のデータ（名前、値、条件、実行内容など）
}

型 式 = {
    種別: 文字列, // "数値", "文字列", "変数", "計算", "比較"
    データ: 任意   // 数値ならf64、計算なら左辺・右辺のBoxなど
}

// 🌟 拡張ポイント: 繰り返し文の定義
// 構文解析（Parser）がこれを作成し、評価器（Evaluator）が実行する